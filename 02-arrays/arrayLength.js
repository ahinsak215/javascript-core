let fruits = ["Apple", "Banana", "Mango"];
console.log(fruits.length);

// This code creates an array named fruits containing three strings: "Apple", "Banana", and "Mango".
// It then logs the length of the array, which is 3.
