let numbers = [10, 20, 30, 40];
console.log(numbers[0]);
console.log(numbers[2]);

// This code demonstrates how to access elements in an array using their index.
// The first console.log outputs the first element (10) and the second outputs the third element (30).
