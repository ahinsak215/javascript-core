const btn = document.getElementById("btn");

btn.addEventListener("click", () => {
  alert("Button clicked!");
});

// Note: The 'click' event is triggered when the user clicks on the button element.
// You can attach 'click' event listeners to other elements as well, such as links, divs, etc.