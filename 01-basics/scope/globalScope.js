let appName = "MERN App";

function showApp() {
  console.log(appName);
}

showApp();

// This code demonstrates global scope in JavaScript.
// The variable appName is accessible both inside and outside the function showApp.