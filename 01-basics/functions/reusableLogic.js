function calculateArea(length, width) {
  return length * width;
}

console.log(calculateArea(5, 10));

// This code defines a function named calculateArea that takes length and width as parameters 
// and returns the area of a rectangle.